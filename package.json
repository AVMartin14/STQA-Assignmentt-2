{
  "main": "bmi.js",
  "scripts": {
    "test": "jest --coverage",
    "test:ci": "jest --ci --coverage --testResultsProcessor=jest-junit",
    "coveralls": "coveralls < coverage/lcov.info"
  },
  "dependencies": {
    "jest": "^27.0.6",
    "jest-junit": "^12.0.0",
    "coveralls": "^3.1.1"
  }
}
