{
  "main": "bmi.js",
  "scripts": {
    "test": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "dependencies": {
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "jest": "^29.5.0"
    "coveralls": "^3.1.0"
  }
}
